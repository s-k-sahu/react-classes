import React, { Component } from "react";

export default class AddingStyledComponent extends Component {
  render() {
    return (
      <div>
        <h1> using sass </h1>
        <h1>i am a component where sass styling is added! </h1>
        <h1>i am a component where sass styling is added! </h1>
        <hr />
      </div>
    );
  }
}
